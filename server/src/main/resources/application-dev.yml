# 开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/company_cms?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: hongyuan2024
  data:
    redis:
      host: localhost
      port: 6379
      password: redis_pass_2024

# 开发环境开启SQL日志
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    com.hongyuan: debug
